spring:
  datasource:
    url: jdbc:mysql://localhost:3306/support_system
    username: root
    password: Kavin@200424

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
